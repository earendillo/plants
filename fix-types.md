Fix all TypeScript errors in the `/lib` directory caused by the recent Supabase integration. We are implementing a strict "Mapping Strategy" to separate DB types from Frontend types.

Context & Rules:
1. FRONTEND TRUTH: `types/index.ts` contains manual `camelCase` types (e.g., `wateringIntervalDays`). Do NOT change these types. They are the source of truth for the UI and utility functions.
2. DATABASE TRUTH: `types/supabase.ts` contains autogenerated `snake_case` types from Supabase (e.g., `watering_interval_days`).
3. THE BARRIER: All files in `/lib/db/` must act as a translation layer. 
   - When fetching data: Map the `snake_case` Supabase response to our `camelCase` types from `types/index.ts` before returning.
   - When mutating data (insert/update): Accept `camelCase` types as arguments and map them to `snake_case` before sending to Supabase.
4. UTILITIES: Fix `/lib/utils.ts` (and any other logic files) to strictly use the `camelCase` types from `types/index.ts`. They should not know about Supabase types.
5. CLEANUP: Remove any temporary types or messy workarounds in `/lib` that violate this boundary.

Task: Implement the mappers in `/lib/db/`, fix `/lib/utils.ts`, and run `npx tsc --noEmit` to verify all type errors in `/lib` are resolved.